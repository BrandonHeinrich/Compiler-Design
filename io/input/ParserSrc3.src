BEGIN
  /* DEPRECIATED
  cow := 12
  chicker := 234
  myFavNumber := 142913828922
  */
  
  a := 3; b := 4;
  x := 5 * (y - 2); h := 3; b := 4;
  g := /* (x*y) + */ (x*b); // Woah!!! g := 60;
  y := z; w:= 1;
  // Distance Function
  varDistance := (a*a) + (b*b);
END
